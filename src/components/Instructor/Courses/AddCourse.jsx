import React from "react";
import { AiOutlinePlusCircle } from "react-icons/ai";

const AddCourse = () => {
    return (
        <div>
            <h4>Title Section</h4>
            <input type="text" />
            <button>
                <AiOutlinePlusCircle />
            </button>
        </div>
    );
};

export default AddCourse;
